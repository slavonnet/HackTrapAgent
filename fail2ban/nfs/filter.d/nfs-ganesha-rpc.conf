[Definition]
# Match repeated NFSv4 SETCLIENTID probes emitted by nfs-ganesha debug logs.
failregex = ^.*nfs_rpc_process_request\s+:DISP\s+:DEBUG\s+:Request from (?:::ffff:)?<HOST> for Program 100003,\s+Version 4,\s+Function 1 has xid=\d+$
ignoreregex =
