# HackTrapAgent
HackTrapAgent is a lightweight Docker Compose honeypot that traps intruders and automatically extracts their IP addresses. It forwards these IPs to the main host via multiple transports, enabling instant blacklisting and proactive defense
